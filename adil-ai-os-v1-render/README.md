# Adil AI OS v1 - Render Ready
This package is intentionally flat (no subfolders) so it can be uploaded from a mobile browser without GitHub flattening its folder structure.

Render settings:
- Root Directory: adil-ai-os-v1
- Build Command: npm install
- Start Command: npm start
- Environment: BUSINESS_NAME, BUSINESS_EMAIL, ADMIN_KEY, optional WHATSAPP_NUMBER
